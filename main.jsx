import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // Abbiamo tolto l'estensione per sicurezza

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
